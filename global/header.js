import Reach from "react";
import "./header.css";

function Header(){
    return(
        // add all the html for the header
        <header class="header">
            <p>this is the header</p>
        </header>

    );
}

export default Header;